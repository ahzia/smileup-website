# HTTPS API URL (https://YOUR_REF.supabase.co) — or use the same Postgres URL as smileup-impactchain (DATABASE_URL / pooler).
SUPABASE_URL=
# Optional fallback if you only define DB URL like impactchain (same value as DATABASE_URL there).
# DATABASE_URL=

SUPABASE_SERVICE_ROLE_KEY=

